#include "main.h"
#include <string.h>

/**
 * _memset - dsgfhf
 * @s: sdff
 * @b: sdsfdgffd
 * @n: sarftred
 * Return: asd
 */
char *_memset(char *s, char b, unsigned int n)
{
	unsigned int a;

	for(a = 0; a < n; a++)
		s[a] = b;

	return (s);
}
